import {sqliteTable,text,integer,index,uniqueIndex} from 'drizzle-orm/sqlite-core';
import {sql} from 'drizzle-orm';
export const members=sqliteTable('members',{id:text('id').primaryKey(),email:text('email').notNull(),name:text('name').notNull(),status:text('status').notNull().default('pending'),createdAt:integer('created_at').notNull(),reviewedAt:integer('reviewed_at')},t=>[index('members_status').on(t.status)]);
export const sessions=sqliteTable('sessions',{id:text('id').primaryKey(),userId:text('user_id').notNull().references(()=>members.id),bank:text('bank').notNull(),questionIds:text('question_ids').notNull(),answers:text('answers').notNull().default('{}'),version:integer('version').notNull().default(0),startedAt:integer('started_at').notNull(),submittedAt:integer('submitted_at'),score:integer('score')},t=>[index('sessions_user_time').on(t.userId,t.startedAt),uniqueIndex('one_active_bank').on(t.userId,t.bank).where(sql`${t.submittedAt} is null`)]);
export const attempts=sqliteTable('attempts',{sessionId:text('session_id').notNull().references(()=>sessions.id),userId:text('user_id').notNull(),questionId:text('question_id').notNull(),bank:text('bank').notNull(),correct:integer('correct').notNull(),answered:integer('answered').notNull()},t=>[uniqueIndex('attempt_once').on(t.sessionId,t.questionId),index('attempts_progress').on(t.userId,t.bank,t.questionId)]);

